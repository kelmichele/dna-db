<% @page_title = "Chatrooms" %>

<div class="full">
  <div class="wrap">
    <div class="col-md-8">
      <ul id="chatrooms-list">
        <% @chatrooms.each do |chatroom| %>
          <%= render 'chatrooms/chatroom', chatroom: chatroom, user: current_user %>
        <% end %>
      </ul>
    </div>

    <div class="col-md-2 text-center">
      <div class="panel panel-primary">
        <div class="panel-heading">
          <h3 class="panel-title">User list</h3>
        </div>

        <div class="panel-body">
          <ul>
            <% @users.each do |user| %>
            <!-- <% if user_signed_in? && user.admin? %><% end %> -->
            <li><%= link_to user.email, chatrooms_path(user_id: user), remote: true, method: :post %></li>
            <br>
            <% end %>
          </ul>
        </div>

      </div>
    </div>
  </div>
</div>


