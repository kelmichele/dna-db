<% @page_title = "Edit #{@town.townname.capitalize}" %>

<div class="full">
	<div class="wrap">
		<h1><%= @page_title %></h1>
		<%= render 'form' %>
		<%= link_to("Back to #{@town.townname}", town_path(@town), :class => 'back-link inline') %>
		<%= link_to("Back to #{@state.name}", state_path(@state), :class => 'back-link inline') %>
	</div>
</div>
