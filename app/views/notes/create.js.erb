var chatroom = $('#chatrooms-list').find("[data-chatroom-id='" + "<%= @chatroom.id %>" + "']");
chatroom.find('.notes-list').find('ul').append("<%= j(render 'notes/note', note: @note, user: current_user) %>");
chatroom.find('textarea').val('');
