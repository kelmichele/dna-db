<% @page_title = @state.name.capitalize %>

<div class="full">
	<div class="wrap">
	  <h1>Get DNA Tested Today in <span><%= @state.name %></span></h1>
	  <hr class="title-line">
		<div class="clearfix"></div>
		<%= link_to("Add New City", new_town_path, :class => 'action new btn-add inline') %>
		<%= render 'shared/dna-text' %>
	</div>

	<div class="all-towns">
		<div class="wrap">

			<table class="listing">
	  		<% if @state_towns != 0 %>
					<% @state_towns.each do |town| %>
					<tr>
					  <td><h4 class="state-name"><%= link_to town.townname.capitalize, town_path(town) %></h4></td>
						<td class="actions">
				      <small><%= link_to("Edit", edit_town_path(town)) %></small>
				    </td>
				  </tr>
		  		<% end %>
	  		<% end %>
			</table>
		</div>
	</div>

	<div class="wrap">
		<%= link_to("Back to States", states_path, :class => 'back-link') %>
	</div>
</div>

 <!-- <% if @state_towns = 0 %><p>Unfortunately, we have no testing locations in <%= @state.name.capitalize %>, but you can still follow the link below, and order an At Home DNA Test Kit Today!</p><% end %> -->
<!--
towns = []
10.times do |i|
  towns << Town.new(:townname => "book #{i}")
end
Book.import books    # or use import!
 -->
