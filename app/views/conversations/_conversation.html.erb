<div class="conv">
  <p>Chatting with <b><%= conversation.with(current_user).name %></b></p>
  <!-- online_status conversation.with(current_user)  -->
  <em><%= conversation.personal_messages.last.body.truncate(25) %></em>
  <br>
  <%= link_to 'View Conversation', conversation_path(conversation), class: 'view-link' %>
  <hr>
</div>
