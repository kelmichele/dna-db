<div class="admin-bar">
	<div class="avail">
		<p>Available for Customer Support</p>
	</div>

	<div class='admenu' id ='chatrooms-menu'>
	  <a class="dropdown-toggle" data-toggle="dropdown" href="#">
	    <i class="fa fa-envelope-o" aria-hidden="true">
	      <span id="unseen-chatrooms"></span>
	    </i>
	  </a>

	  <ul class="dropdown dropdown-menu" role="menu">
	  	<% @users.each do |user| %>
	      <li><%= link_to user.name, chatrooms_path(user_id: user), remote: true, method: :post %></li>
      <% end %>
	  </ul>
	</div>

</div>
